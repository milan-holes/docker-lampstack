# docker-sync installation
gem install docker-sync

# docker-sync strategy - unison installation
brew install unison
brew install eugenmayer/dockersync/unox

# fswatch installation
brew install fswatch